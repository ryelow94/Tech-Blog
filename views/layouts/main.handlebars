<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" type="text/css" href="/css/style.css"> 
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>

<body class="has-background-light">
  <header>
    <h1 class="title has-text-primary-dark">Tech Blog</h1>Welcome to the Tech Blog</h1>
  </header>
  <nav>
    <a class="button" href="/">Home</a>
    <a class="button" href="/signup">Sign Up</a>
    {{#if loggedIn}}
    <button id="logout" class="button has-text-danger-dark">Logout</button>
    <a id="dashboard-btn" class="button has-text-success" href="/dashboard">Dashboard</a>
    {{else}}
    <a class="button" href="/login">Login</a>
    {{/if}}
  </nav>
  <main>
    <div>
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>